server:
 port: 8888
spring:
 application:
  name: config-server
 cloud:
  config:
   server:
    git:
     uri: https://github.com/RafaBlanco95/SpringConfigurationServer.git
    default-label: master
    search-paths:
    - '*service*'
